#!/usr/bin/env bash
# boxctl alias wrapper - forwards to boxctl

SCRIPT_PATH="$(readlink -f "${BASH_SOURCE[0]}")"
SCRIPT_DIR="$(dirname "$SCRIPT_PATH")"
BOXCTL_ROOT="$(dirname "$SCRIPT_DIR")"

exec "$BOXCTL_ROOT/bin/boxctl" "$@"
